apiVersion: v1
kind: Service
metadata:
  name: my-kube-service
spec:
  type: LoadBalancer  # Expose service externally
  selector:
    app: my-kube-app  # ✅ matches the Deployment label
  ports:
    - protocol: TCP
      port: 8082       # External port
      targetPort: 3000 # Internal container port
